# conferencer
Conferencer Main Repo

## Quick Start

To launch the application, simply run:

```bash
./launch.sh
```

This script will:
1.  Start a MongoDB container (requires Docker).
2.  Set up and start the Backend (Flask).
3.  Set up and start the Frontend (Vite).

## Prerequisites

- Docker
- Node.js (npm)
- Python 3
